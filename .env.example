# Copy this file to .env and adjust if needed.
PORT=3001
VITE_SIGNALING_URL=http://localhost:3001
# Origin the client is served from — used for CORS on both HTTP and Socket.IO.
CLIENT_ORIGIN=http://localhost:5173
